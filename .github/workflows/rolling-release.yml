name: Rolling Release

on:
  push:
    branches: [ main ]

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  build:
    needs: ci
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          VERSION="rolling-${GITHUB_SHA:0:7}"
          COMMIT="${GITHUB_SHA:0:7}"
          BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          VERSION_PKG="github.com/lmorchard/tabstack-go-cli/cmd"
          LDFLAGS="-X $VERSION_PKG.version=$VERSION -X $VERSION_PKG.commit=$COMMIT -X $VERSION_PKG.date=$BUILD_DATE"

          go build -ldflags "$LDFLAGS" -o tabstack${{ matrix.goos == 'windows' && '.exe' || '' }}

      - name: Package (Unix)
        if: matrix.goos != 'windows'
        run: |
          tar czf tabstack-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz tabstack

      - name: Package (Windows)
        if: matrix.goos == 'windows'
        run: |
          7z a tabstack-${{ matrix.goos }}-${{ matrix.goarch }}.zip tabstack.exe

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tabstack-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            tabstack-*.tar.gz
            tabstack-*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Generate checksums
        run: |
          find . -name "tabstack-*" -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec sha256sum {} \; > checksums.txt

      - name: Delete existing rolling release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete latest --yes || true
          git push origin :refs/tags/latest || true

      - name: Create Rolling Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Rolling Release
          body: |
            **Automated rolling release built from the latest commit on the main branch.**

            ⚠️ This release may be unstable and is intended for testing purposes only.

            **Commit:** ${{ github.sha }}
            **Built:** ${{ github.event.head_commit.timestamp }}
          prerelease: true
          files: |
            tabstack-*/*.tar.gz
            tabstack-*/*.zip
            checksums.txt

  # Uncomment to build and push Docker image
  # docker:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #
  #     - name: Build and push
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: yourusername/tabstack:latest
